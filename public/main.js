const request = require('request');

function makeTable(){
    const table = document.getElementById('myTable');
    const input = document.getElementById('searchPoke').nodeValue;

    console.log(input);
}

for(
)

document.getElementById('clickMe').addEventListener("click", makeTable);


